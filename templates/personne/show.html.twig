{% extends 'base.html.twig' %}

{% block title %}Personne{% endblock %}

{% block body %}
    <h1>Personne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPers</th>
                <td>{{ personne.idPers }}</td>
            </tr>
            <tr>
                <th>NomPers</th>
                <td>{{ personne.nomPers }}</td>
            </tr>
            <tr>
                <th>PrenomPers</th>
                <td>{{ personne.prenomPers }}</td>
            </tr>
            <tr>
                <th>DataNaissance</th>
                <td>{{ personne.dataNaissance ? personne.dataNaissance|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('personne_index') }}">back to list</a>

    <a href="{{ path('personne_edit', {'idPers': personne.idPers}) }}">edit</a>

    {{ include('personne/_delete_form.html.twig') }}
{% endblock %}
