{% extends 'base.html.twig' %}

{% block title %}Residence{% endblock %}

{% block body %}
    <h1>Residence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdResi</th>
                <td>{{ residence.idResi }}</td>
            </tr>
            <tr>
                <th>NomResi</th>
                <td>{{ residence.nomResi }}</td>
            </tr>
            <tr>
                <th>AdresseResi</th>
                <td>{{ residence.adresseResi }}</td>
            </tr>
            <tr>
                <th>NombreAppart</th>
                <td>{{ residence.nombreAppart }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('residence_index') }}">back to list</a>

    <a href="{{ path('residence_edit', {'idResi': residence.idResi}) }}">edit</a>

    {{ include('residence/_delete_form.html.twig') }}
{% endblock %}
